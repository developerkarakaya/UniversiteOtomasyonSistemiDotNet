
@{
    ViewBag.Title = "Fakülte Ekle";
}
@using UniversiteOtomasyon.DataEntities;
<link href="https://cdnjs.cloudflare.com/ajax/libs/sweetalert/1.1.3/sweetalert.min.css" rel="stylesheet" type="text/css" />
<script src="https://cdnjs.cloudflare.com/ajax/libs/sweetalert/1.1.3/sweetalert.min.js"></script>
<div class="row">
    <div class="col-sm-12">
        <div class="white-box">
            <h3 class="box-title">Fakülte Ekle</h3>
            <form class="form-material form-horizontal" action="@Url.Action("Ekle","Fakulte")" method="post" enctype="multipart/form-data">
                <div class="form-group">
                    <label class="col-md-12" for="example-text">
                        <span> Fakülte Adı</span>
                    </label>
                    <div class="col-md-12">
                        <input type="text" id="FACULTYNAME" name="FACULTYNAME" class="form-control" placeholder="Fakülte Adı Giriniz" required>
                    </div>
                </div>
               
                <button type="submit" class="btn btn-success waves-effect waves-light m-r-10">Kaydet</button>
                <a href="/fakulte-listesi" class="btn btn-inverse waves-effect waves-light">Listeye Dön</a>
            </form>
        </div>
    </div>
</div>

<script>
    if ('@ViewBag.message' == 1) {
        ShowMessage("İşlem Başarıyla Gerçekleşti", 's');
    }
    else if ('@ViewBag.message' == 2) {
        ShowMessage("İşlem Gerçekleşirken Hata Meydana Geldi", 'e');
    }
    else {

    }


        function ShowMessage(message, type) {
            if (type == "s") {
                swal({
                    title: 'İşlem Başarılı',
                    text: message,
                    type: 'success',
                    showCancelButtonClass: 'btn-success',
                    confirmButtonText: 'Tamam'
                });
            }
            else {
                swal({
                    title: 'İşlem Başarısız',
                    text: message,
                    type: 'error',
                    showCancelButtonClass: 'btn-danger',
                    confirmButtonText: 'Tamam'
                });
            }
        }
</script>
